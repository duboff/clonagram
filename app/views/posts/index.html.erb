<% if @posts.any? %>
  <% @posts.in_groups_of(3, false) do |triplet| %>
    <div class="row">
      <%= render triplet %>
    </div>
  <% end %>
<% else %>
  <h2>No Photos yet!</h2>
  <%= link_to 'Add a post', new_post_path, class: 'btn btn-default' %>
<% end %>
